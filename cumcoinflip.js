function coinFlip() {
function flip() {
    return Math.floor((Math.random() * 2) + 1)} 
    let result = flip(); 
    if (result===1) {
        winner= 'HEADS'
        message.channel.send(`Je hebt **${winner}** gecummed`);
        } 
    else {
         verliezer = 'HEADS'
         message.channel.send(`Je hebt **${verliezer}** gecummed`); 
         }
    { 
     (message.content === `${prefix}poep`) 
        message.channel.send ();
    } 
}

            